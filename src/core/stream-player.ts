import { ChildProcess, spawn } from 'child_process';

export class StreamPlayer {
    private _proc = null;


    public async play(){
        // this._proc = spawn();
    };
    public stop(){};
}